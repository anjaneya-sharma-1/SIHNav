# Environment Configuration for SIH Classification Pipeline

# Required: Gemini API Key
# Get yours from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# Optional: Gemini model to use (default: gemini-1.5-flash)
GEMINI_MODEL=gemini-1.5-flash

# Optional: Batch processing configuration
# Gemini Flash Lite allows 30 requests per minute
BATCH_SIZE=30
BATCH_INTERVAL_SEC=60

# Optional: Retry configuration
LLM_MAX_RETRIES=3
LLM_BACKOFF_BASE=2.0
LLM_BACKOFF_INITIAL=1.0
LLM_JITTER_SEC=0.3
